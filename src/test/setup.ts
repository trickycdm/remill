/**
 * Vitest global setup. remill has no external service dependencies to mock in
 * unit tests (D1 is provided in-memory via createTestD1, R2 is stubbed per-test).
 * Kept as a seam for future global test config.
 */
export {};
