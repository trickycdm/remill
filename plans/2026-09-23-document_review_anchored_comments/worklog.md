# Worklog — 2026-09-23-document_review_anchored_comments

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-09-23 | START | Part 1 — optimistic concurrency (D54), branch `trickycdm/optimistic-concurrency` |
| 2026-09-23 | FIX | Drizzle renders single-table selects unqualified, so the correlated `MAX(revision)` subquery bound `"id"` to `document_revisions.id` (always 0 → every save collided). Hand-qualified the subquery's column names |
| 2026-09-23 | RE-PLAN | Dropped `nextRevisionNumber` — the new revision is `existing.revision + 1` from the read that already happens, one fewer query per save |
| 2026-09-23 | DONE 1a–1c | Service + MCP/REST/admin surfaces + OpenAPI + D54 entry + API_AND_MCP_STANDARDS. 12 new unit tests (service, parser, REST ETag/If-Match, MCP stale result) |
| 2026-09-23 | VERIFIED | type-check 0, lint clean, 655/655 unit. e2e admin-content/revision-diff/editor-islands: all pass, but `admin-content` "full lifecycle" flakes ~50% under `--repeat-each` at steps that don't touch D54 code (CodeMirror fill on the create page; delete dialog open) — not verified against main |
| 2026-09-23 | RE-PLAN | Single PR for all parts (user). Part 1 stays on `trickycdm/optimistic-concurrency` with the rest |
| 2026-09-23 | FIX | Production deploys run migrations but never re-run seed.sql, so the `comment` role_permissions rows ship in migration 0017 too (`INSERT OR IGNORE … WHERE EXISTS` role — a no-op on a fresh DB, where seed.sql adds them). Upgrade-path test in seed.test.ts |
| 2026-09-23 | DONE 2a–2c | Migration 0017 (comments, review_reviewers, item_grants.review_mode, CHECKs), `comment` action (admin/editor; author on own), queries/comments.ts, services/comments (threads, visibility rule, review links, mode flip preview), comment.* events |
| 2026-09-23 | RE-PLAN | Reviewer identity on a personal link needs no cookie — the link's one invited reviewer IS the identity; only open links use `rm_reviewer`. Revoking a link cascades its reviewers; their comments keep `author_name` and drop to owner-only visibility |
| 2026-09-23 | DONE 3a–3b | src/lib/anchor (DOM-free text rules shared with the island, htmlparser2 extractor, locate/resolve/relocate) + re-anchor hook in updateDocument (best-effort, logged). New dep: htmlparser2 |
| 2026-09-23 | VERIFIED | type-check 0, lint clean, 693/693 unit (15 anchor, 21 comments service, 2 migration) |
| 2026-09-23 | RE-PLAN | `render=review` is served by the comments service (`renderReview` + pure `review-brief.ts`), not registered in `renders.ts` — the TEXT_RENDERS registry is per-template and pure, while the review brief needs comment data and applies to ANY collection with html/markdown fields. The brief carries each quote in context rather than reproducing the document (the agent fetches the source with `get_<slug>`). Resolve/reopen merged into one `resolve_comment_<slug>` tool (`reopen: true`) |
| 2026-09-23 | DONE 6 | MCP: comments_/comment_/reply_comment_/resolve_comment_<slug> (gated `comment` + annotatable fields), `review` render on get_<slug>, `resolves` on update_<slug> (validated BEFORE the save). REST: /comments, /comments/:id (DELETE), /replies, /resolve, `?render=review`. OpenAPI entries |
| 2026-09-23 | RE-PLAN | No `wants.annotations` template flag: every template renders fields through `FieldView`, so wrapping public html/markdown output there (`[data-rm-annotatable][data-rm-field]`) marks every template's prose at once. Raw mode (D27) stays unannotatable — it bypasses FieldView |
| 2026-09-23 | RE-PLAN | Edit-page Comments card is a summary with deep links into the overlay (`?preview=1&review=1#comment-<id>`), not a second place to reply/resolve — commenting happens next to the text. Resolve/reply live in the overlay (and MCP/REST) |
| 2026-09-23 | DONE 4 | Overlay: `ReviewPanel` (one component for reviewer + principal, Datastar form posts answered with the whole panel), `src/lib/review-http.tsx` route plumbing, reviewer endpoints under /s/:token/review/*, principal endpoints under /admin/c/:c/:id/review/*, `rm_reviewer` signed cookie, review rate tiers, island `src/client/review.ts` (canonical-text mirror, selection → composer, CSS Custom Highlight API, card↔passage navigation) |
| 2026-09-23 | FIX | Datastar's morph carries any element whose id matches — live form state included — even into a new parent, so a posted comment's target/quote leaked into the next comment. Composer + control ids are now per-render; the island finds the composer by attribute. Same for reply boxes. The island also clears the consumed selection after a post |
| 2026-09-23 | DONE 5 | Share card review-link section (personal/open, mode, reviewers + done, copyable URL, revoke), mode-flip confirmation page with counts, edit-page Comments card. Labels worded so they don't substring-collide with the share-link form's |
| 2026-09-23 | VERIFIED | type-check 0, lint clean, 699/699 unit, build OK. e2e `document-review.spec.ts` 7/7 (text + figure + script-text comments, highlights, open-link naming, group/individual visibility, flip warning, owner reply/resolve, GUI edit → outdated, stale save refused; axe on the panel). `visibility-and-share-links` passes. Migration 0017 applied to the existing local D1 added the three `comment` rows (the real upgrade path) |
| 2026-09-23 | NOTE | Full e2e run on the long-lived local D1: 10 failures outside this feature traced to accumulated state from earlier runs (existing `Moderator` role, `widgets`/`companies` collections, duplicate `e2e-bot`, `allowCdnScripts` left on, login rate limit) and the known CodeMirror-fill flake; one axe `link-in-text-block` hit on /admin/access's existing "connect it →" link, rendered only when a tokenless agent exists. Not verified against a fresh D1 |
| 2026-09-23 | FOLLOW-UP | Discoverability after first live use (v1.14.0): reviewers got no signal they could comment and the owner's way in was a hand-typed query string. Added a reviewer invite banner above the article on review links, the owner's "Show/Hide comments" toggle in the preview banner, a how-to line in the panel, a sticky "Comments (n)" jump button on narrow screens (the panel flows after the article there), and "Read only" / "Can comment" labels in the Share card. e2e 8/8 incl. a 390px-wide reviewer; full-page axe clean at desktop + mobile |
