# Worklog — 2026-07-07-platform_completion_tiered_roadmap

| Timestamp | Action | Detail |
|-----------|--------|--------|
| 2026-07-07 22:26 | plan created | 11-phase Tiers 1–3 roadmap planned + user-approved; 4 design forks decided (trash table, native media picker over Uppy/D12, search page over cmd-K palette, per-collection events access); written for execution by a fresh session — all facts verified against code 2026-07-07 |
| 2026-07-07 23:20 | Phase 1 COMPLETE | Filter operators (eq/gte/lte/contains/in) + FTS5 search on all surfaces (REST ?q=, MCP search_<slug> + list filters, /admin/search + header box + '/' shortcut, rebuild button). Migration 0007; toSearchText hook; D28 logged; steering updated. 265 unit / 39 e2e green (branch feature/platform-completion-tier1) |
| 2026-07-07 23:20 | RE-PLAN (minor) | Two plan-spec corrections found during implementation: (1) D1 driver cannot batch raw db.run(sql) — FTS sync uses prepared statements via out-of-schema Drizzle handle src/db/fts-table.ts instead; (2) reused existing pickTitleField heuristic instead of adding titleFieldOf to def-helpers (Phase 6 will reuse it). Test D1 shim made Promise-shaped (drizzle raw-query path chains .then) |
| 2026-07-07 23:40 | Phase 2 COMPLETE | Cron infra (2 triggers, scheduled() export, src/jobs dispatcher, main.tsx app→named export) + document_trash (migration 0008 w/ CHECK): snapshot-then-delete, restore under original id, /admin/trash + nav, REST /api/trash, MCP delete_<slug>, 30d purge job, collectionsWithAction helper. D29+D31 logged; ACCESS_CONTROL/DATABASE/API steering updated. 273 unit / 41 e2e green |
| 2026-07-07 23:40 | security addition | Beyond plan spec: trash listing compiles own/published delete-conditions in-query (TrashScope) and restore/destroy authorize the item decision with snapshot createdBy/status — otherwise an own-conditioned deleter could see/restore others' trash |
| 2026-07-07 23:55 | Phase 3 COMPLETE | MCP parity: upload_media (base64, strict decode, McpToolContext threads R2+limiter, shared 'upload' bucket keyed tokenId-else-IP), revisions_<slug> (read), restore_<slug> (update), /mcp cap 8MiB, consumeRateLimit extracted. D34 logged; API/SECURITY steering updated. 7 new tests (mcp-parity, base64) |
| 2026-07-08 00:15 | Phase 4 COMPLETE | Audit surfacing: audit_log.collection column (migration 0009, stamped by authorize), listAuditPage (filters + keyset), /admin/activity + nav + Activity icon, dashboard recent-activity card, REST /api/audit, MCP list_audit, token last-used on Access cards. ACCESS_CONTROL/API steering updated (no new decision — reuses manage_access). 283 unit / 44 e2e green |
| 2026-07-08 00:15 | a11y component fix | Shared Table wrapper made a focusable labelled region (tabindex+role+aria-label) — wide tables triggered axe scrollable-region-focusable on /admin/activity; fixed at the component so every table passes |
| 2026-07-08 00:15 | TIER 1 DONE | Phases 1–4 all complete and verified on feature/platform-completion-tier1 (uncommitted). Next session: Tier 2 = Phases 5–8 (scheduled publishing, discovery pack, events outbox, import/export) |
| 2026-07-08 00:30 | COMPLETE | Tier 1 wrapped up (plan finalized PARTIAL 4/11; Tiers 2–3 remain) |
| 2026-07-08 07:20 | Tier 1 committed | 7f8815d on feature/platform-completion-tier1; Tier 2 execution planned (plans/.tmp overlay; anchors re-verified post-Tier-1) |
| 2026-07-08 07:59 | Phase 5 COMPLETE | Scheduled publishing: publish_at (migration 0010 + partial index), system actor (D30: 'system' in Surface/kind, systemPrincipal, authorize by-kind branch that still audits), scheduleDocument (narrow setPublishAt — no revision), drainScheduledPublishes in PER_MINUTE cron, sidebar schedule UI + stale delete-copy fix, REST /schedule, MCP schedule_<slug>, publishAt in payloads, 'system' in activity/OpenAPI surface filters. D30+D32 logged; ACCESS_CONTROL/API/DATABASE steering updated. 295 unit (+12) + 6 mcp-parity / 46 e2e (+2) green |
| 2026-07-08 07:59 | RE-PLAN (minor) | Two spec deviations: (1) drain lives in services/documents (jobs call services ONLY — purgeExpiredTrash precedent; no src/jobs/publish.ts); (2) system-actor revisions carry saved_by NULL — document_revisions.saved_by FKs principals and the system actor deliberately has no row; audit row (surface 'system') is the attribution |
| 2026-07-08 08:25 | Phase 6 COMPLETE | Public discovery: /rss.xml (+?collection narrowing), /sitemap.xml, /robots.txt hand-registered in main.tsx; / homepage when publicRead+lifecycle collections exist (else admin redirect stays); CC-3 head threading (PageHead + ContextRenderer augmentation, c.render(content, head)); OG/canonical/feed meta on public doc pages; new src/services/discovery (anonymous gated reads), src/lib/feeds.ts (pure XML), src/lib/def-helpers.ts (titleFieldOf extracted from pickTitleField + titleOf/publicUrlOf/excerptFrom); publishAt added to RESERVED_FIELD_KEYS. D35+D36 logged; SECURITY steering updated. 309 unit (+13+ Phase 5's) / 51 e2e (+5) green; manual curl verified (rss CT, ghost-collection 404, robots) |
| 2026-07-08 08:25 | RE-PLAN (minor) | Feed/homepage data assembly lives in a new services/discovery module (plan named only lib/feeds.ts) — routes must not compose gated reads themselves; excerpt extraction stays in the service via buildSearchText, def-helpers stays type-only-pure (no fields/registry import from lib) |
| 2026-07-08 08:50 | Phase 7 COMPLETE | Events outbox: events table (migration 0011, AUTOINCREMENT seq, no FKs, pointer-only), EventInput threaded through every mutation batch (documents insert/update, trash/restore, media + collection singles became batches; setPublished/drain instrumented free), GET /api/events {data,nextSince}, MCP poll_events (all principals), per-collection read filtering = collectionsWithAction ∪ scope-masked publicRead (empty page — not 403 — for unreadable narrowing), pruneEvents daily job + EVENTS_RETENTION_DAYS. D33 logged; DATABASE/API steering updated. 314 unit (+5) + 7 mcp-parity / 51 e2e green; manual anon curl verified (publicRead-only filtering incl. seq gaps, since=-2 400, unreadable narrow → empty) |
| 2026-07-08 09:45 | Phase 8 COMPLETE | Import/export + snapshot: NDJSON format (header=full def, then document lines) via src/lib/ndjson.ts + src/services/transfer; export read-FILTERED (REST /api/c/:slug/export + admin download); import upsert-by-preserved-id through the full pipeline (10 MiB cap, 'import' bucket 10/60s, ?dryRun, per-line errors, header-slug match, def never mutated), admin import page (+axe sweep); snapshotSite → R2 snapshots/<ISO>/ (all collections + media metadata + manifest) via settings button, manage_schema-gated; Export/Import buttons on collection lists; createDocument gained internal CreateOverrides {id,status,createdAt,publishedAt}. D37 logged; API/SECURITY steering updated. 323 unit (+9) / 53 e2e (+2) green; manual verified (snapshot keys in local R2, anon export = published-only, session-curl export 200 NDJSON) |
| 2026-07-08 09:45 | security addition | Beyond plan spec: import lines with status 'published' require the `publish` action (authorized once per run) — without it, import is a publish-permission bypass ("agent drafts, human publishes" must survive bulk ingestion). Foreign createdBy discarded (importer is creator) |
| 2026-07-08 09:45 | RE-PLAN (minor) | Deviations: (1) import-create preserves id/status/createdAt/publishedAt but NOT updatedAt (= import time — truthful, keeps revision savedAt sane); (2) snapshot also writes media.ndjson metadata (plan implied metadata-only note; realized as a file); (3) drive-by fix: getSettings treats defaultPageSize<1 as unset — an e2e settings save stored 0 (empty Datastar number signal) and every list page silently clamped to one-row pages; (4) e2e gotcha: Playwright page.request does NOT carry the session cookie — use in-page fetch for authed admin endpoints |
| 2026-07-08 13:45 | Phase 9 COMPLETE | Editor islands (D38): CodeMirror 6 markdown island (implements D13 — sr-only textarea carrier, native input-event sync per §g, var(--color-*) token theme, deps codemirror+@codemirror/lang-markdown only) + media-picker island (supersedes D12/Uppy — Dialog shell per field, fetch+innerHTML fragment GET /admin/media/picker, in-dialog upload POST → JSON {id} → select-on-upload, no nested form / type=button only inside #editor-form); Scripts in the two editor route files; islands code-split (markdown 511KB route-only, picker 2KB). DATASTAR_PATTERNS worked examples + DESIGN_SYSTEM island-token rule; D38 logged. 323 unit / 56 e2e (+3) green |
| 2026-07-08 13:45 | RE-PLAN (minor) | Deviations: (1) picker fragment loads via plain fetch+innerHTML, NOT plan.md's Datastar @get — the repo bans @get load-fragments (datastar-response.ts; §g gotcha); (2) in-dialog upload is POST /admin/media/picker (co-located), not /picker/upload; (3) e2e fallout: getByLabel on markdown fields now strict-violates (hidden textarea + CM textbox both match) — new e2e/helpers/editor.ts fillMarkdown(role-based) adopted across 7 specs incl. platform-graph-publish's createDoc which auto-detects [data-md-editor]; sr-only elements still count as "visible" to Playwright (assert the class, not visibility) |
