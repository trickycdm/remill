# Technical Decisions — remill

> The seeded decision log (an ADR-style record). Seeded from the build plan §6. Add a new numbered row
> when a load-bearing decision is made or changed; never rewrite history in place — supersede with a new
> entry that references the old one. The policy-model watch-items (plan §8) require a decision-log entry
> before the closed action vocabulary or condition enum is extended.

| # | Decision | Rationale | Alternatives rejected |
|---|---|---|---|
| D1 | New repo, new code; port Blogmill concepts only | Blogmill's substrate (globals, Express, MySQL, jQuery) is incompatible with Workers; its security posture is disqualifying | Incremental port (no viable path) |
| D2 | Collections as data, field types as code | Runtime content modeling is what makes MCP schema tools possible; field types need real code (validators, UI) | All-code schemas (Blogmill/Payload style — redeploy per change); all-data (nowhere for validation/UI logic to live) |
| D3 | Markdown-first rich text | Agent-legible, diff-able, portable; TinyMCE HTML blobs are the single most dated thing in Blogmill | HTML WYSIWYG (opaque to agents); Portable-Text-style JSON AST (heavier than v1 needs; revisit if block editing is wanted) |
| D4 | D1 + EAV index table for querying JSON content | Fixed schema stays tiny; dynamic fields can't use static columns; v1 query needs are modest | Per-collection tables w/ runtime DDL (migration hell); DO-SQLite-per-collection (complexity without a v1 payoff) |
| D5 | Single tenant, single site | Lightweight is the product; multi-tenancy multiplies auth, storage, and billing complexity everywhere | Multi-site (revisit only with a concrete need) |
| D6 | Headless-first; admin included, public rendering deferred | Admin is the product surface users touch daily; public rendering is cleanly separable as an API consumer | Bundled theme system v1 (Blogmill's scope trap) |
| D7 | Standalone single-package repo | dpt-platform's own monorepo platform layer is unwired stubs by its own admission | Adopting the dpt monorepo/turbo layout |
| D8 | Datastar SSR admin, no React | Proven in dpt-platform; tiny payload; islands cover the gaps (editor, uploads) | React/Next admin SPA (contradicts lightweight); HTMX (dpt already migrated off it) |
| D9 | Sessions for humans, bearer tokens for machines | Encrypted cookie needs no external store; tokens are revocable/scoped and MCP-friendly | JWT-everywhere (Blogmill's weak spot); OAuth provider (overkill single-tenant) |
| D10 | MCP via Cloudflare `agents` SDK on a DO | The supported Workers MCP path; DO gives session state | Hand-rolled MCP transport; premature tool-abstraction layer (dpt built one and deleted it) |
| D11 | R2 originals + variant-ready URL scheme; transforms deferred | `/media/:id/:variant` route shields consumers; Cloudflare Image Transformations can slot in without URL changes | Cloudflare Images/Stream from day one (cost/complexity before need) |
| D12 | Uppy for uploads | Maintained, excellent UX, direct-to-Worker multipart; the one Blogmill dependency worth keeping | Hand-rolled `<input type=file>` (poor resumability/progress) |
| D13 | CodeMirror 6 markdown island | Lightweight, no contenteditable fights, agents and humans see identical content | TinyMCE/Tiptap WYSIWYG v1 (heavy; revisit as an *additional* editor later) |
| D14 | Validation whitelist from field descriptors on every write path | Direct fix for Blogmill's mass-assignment vulnerability; one pipeline, three doors | Per-surface validation (drift guarantees a hole) |
| D15 | Humans and agents unified as principals; tokens belong to principals | One authorization pipeline; per-agent identity, revocation, and audit attribution; no shared keys | Separate "API key" model (unattributable writes); OAuth per agent (overkill single-tenant) |
| D16 | Default-deny, additive-only: RBAC (roles-as-data, scoped assignments, tiny condition enum) + per-item grants; no negative rules | Comprehensible and auditable; per-item precision without a policy language; "agent drafts, human publishes" falls out naturally | Full ABAC/policy DSL (complexity kills auditability); pure RBAC (no per-item control); external Zanzibar-style service (not lightweight) |
| D17 | Authorization enforced by witness types + SQL-compiled list filters | Bypassing `authorize()` is a compile error; paginated lists structurally can't leak (types/CI over convention) | Convention-only "services should check" (guarantees drift); in-memory post-filtering (leaks under pagination) |
| D18 | MCP served as a direct streamable-HTTP JSON-RPC endpoint (`src/mcp/handler.ts` + `tools.ts`), NOT an `agents`-SDK `McpAgent` on a Durable Object (supersedes D10's transport) | The `agents` SDK is a §8 fast-moving watch-item and its bearer-auth prop injection is version-fragile; a stateless, generated-tool server needs no DO session state. The load-bearing properties (tools generated from field descriptors, intersected with the principal's permissions, routed through the shared service pipeline) are preserved and unit-tested; the moving dependency stays contained to one thin module | `McpAgent` DO (couples MCP to a fast-moving SDK + adds DO session state for no gain here); a separate MCP worker (not lightweight, splits the one-pipeline invariant) |
| D19 | Personas as nullable `principals.subtype` ∈ {person, service, agent} — display/grouping only; security stays on `kind` (`user` \| `agent`) | Lets the UI distinguish a human, a data-pulling system, and an AI agent without touching the permission model; `refuseAgentEscalation` keys off `kind` and correctly covers services too; non-breaking backfill | A third security `kind` (ripples through every authorization decision for zero security gain); separate personas table (overkill for a display axis) |
| D20 | Email delivery behind an `EmailTransport` interface (`src/lib/email/`) with a console stub that logs redacted metadata + the action link — no real provider yet. *(Realized 2026-07-06: `ResendEmailTransport` selected when `RESEND_API_KEY` + a From exist; From precedence `settings.emailFrom` > `env.EMAIL_FROM`; styled templates in `src/lib/email/templates.ts`; stub remains the keyless/test default; sends are best-effort, never thrown.)* | Invite and share-by-email flows ship and are testable now; PII never logged; a provider (Resend/MailChannels) can be slotted in behind the same interface without touching callers | Blocking invites on provider selection; sending directly from services (untestable, unswappable) |
| D21 | Relations are `document_index` rows: the `relation` field's `toIndex` emits the target doc id(s), with the engine extended to allow one index row **per array element** — no separate edges table | One indexing path for all field types; backlinks are a reverse lookup on the same table the forward index already uses; multi-relations fall out of the same contract. *(Accepted; lands in roadmap Track B.)* | A dedicated `edges` table (second write path to keep in sync, second access story); storing arrays as JSON only (not reverse-lookupable) |
| D22 | Share links reuse `item_grants` with `subjectKind='link'` (hashed link token as `subjectId`; hashing per `src/lib/token.ts`) — no parallel permission system, and no migration (`subject_kind` has no CHECK constraint) | An outsider's read is decided by the same `getApplicableGrants`/`compileReadFilter` machinery as every other grant — expiry, revocation, and audit come free; the token is plaintext-once like API tokens | A separate share_links table + bespoke checks (parallel permission system — the exact smell A5 removed); signed URLs (no revocation) |
| D23 | Optional `ViewComponent` on the `FieldType` contract for read/detail/public surfaces, defaulting to safe escaped text; markdown renders via **micromark** (+GFM) — raw HTML escaped, dangerous protocols stripped, no separate sanitizer dep | The render seam stays inside the one field contract (the schema engine's seventh consumer, not a parallel template system); unsafe-by-default is impossible — only a type that opts in renders anything but escaped text; micromark is safe by construction on Workers (no DOM) | A template/theme engine (Blogmill's scope trap, D6); rendering in routes per collection (defeats schema-drivenness); `marked` + DOM-based sanitizer (DOMPurify needs a DOM shim on Workers) |
| D24 | Teams as a fourth item-grant subject kind (`subjectKind='team'`) backed by `teams`/`team_members`, plus multi-use expiring `team_invites` join links (`rmj_` tokens, role preset picked at mint, optional max-uses, expiry required ≤90d, principal created at acceptance) | "Share with the tech team" is subject RESOLUTION, not decision logic: membership resolves to `teamIds` beside `roleSlugs` in `subjectMatchFor`, so `decide()`, expiry, revocation, matrix visibility, and audit all come free; join links reuse the hash-at-rest/plaintext-once token discipline | Teams-as-roles (conflates capability with audience — a team must never carry permissions); widening `invite_tokens` (principal-bound `NOT NULL` FK + single-use `consumedAt` contradict a link whose principal doesn't exist yet) |
| D25 | A trusted `html` field type whose `ViewComponent` renders the value VERBATIM via `dangerouslySetInnerHTML` — the second sanctioned XSS exception (scoped reversal of D3's no-HTML-blobs stance for charts/visuals markdown can't express); trust = collection-level write permission (FieldAccess is reserved in v1), and the field is writable over REST/MCP by design | The platform's own authors (human or agent) are trusted to publish full pages with executable inline script under the public CSP; the exception stays inside the one field contract and is loudly documented at the type | A server-side sanitizer (no DOM on Workers — D23 — and it would strip exactly the scripts/SVG that motivate the feature); a separate "pages" subsystem outside the schema engine |
| D26 | New closed-vocabulary action `share_link`; `createShareLink` re-gated from `manage_access`+`refuseAgentEscalation` to `authorize('share_link', {collection, documentId})`; admin+editor system roles hold it; the MCP `share_link_<slug>` tool mints read-only links with a REQUIRED expiry clamped to 30 days | Link-minting is not access management: splitting it out lets a human grant an agent exactly "may share this" (via role or one-document item grant) while agents still never hold `manage_access` (SEC-8 untouched on every identity/role/token mutation); audit rows become self-describing (`share_link@mcp`) | A per-principal boolean flag (a parallel permission system outside roles/grants/scope); token-scope-only (the mask can only narrow — it cannot confer); leaving it under `manage_access` (all-or-nothing forces over-granting) |
| D27 | Per-collection `renderMode: 'shell' \| 'raw'` (raw = the first `html` field IS the standalone page, served via `c.html`, shell fallback when empty) + the security headers forked per surface: protected prefixes always strict; PUBLIC paths widen `script-src` to an exact CDN allowlist (jsdelivr/unpkg) only while the admin-settable, warning-labelled `allowCdnScripts` setting is on; Chart.js vendored under `/vendor/` so charts work with the toggle OFF | Full-page HTML needs the schema-level altitude (per-document flags would fragment rendering; global CSP widening would weaken the admin for a public-page feature); the prefix classifier + settings read is one PK lookup on public paths only | Global CSP widening (admin pays for a public feature); per-document render flags (wrong altitude); iframes/sandboxing (breaks the "the document IS the page" model) |
| D28 | List filter operators (`?filter[field][op]=`, op ∈ eq/gte/lte/contains/in, compiled to `document_index` subqueries) + full-text search via a **plain FTS5 table** `document_fts` — DDL lives only in migration 0007 (never schema.ts; drizzle-kit would emit spurious DDL), addressed through the out-of-schema Drizzle handle `src/db/fts-table.ts`, synced as PREPARED statements inside the atomic write batch (D1 batches cannot carry raw SQL), bm25-ranked (title 5×), per-collection ACL composed in-query. Surfaces: REST `?q=` + operator filters, generated MCP `search_<slug>` + `filters` on `list_<slug>`, admin header box + `/admin/search`. New optional FieldType hook `toSearchText` supplies FULL text (toIndex truncates markdown/html to a 200-char lead-in — `contains` only sees that lead-in; `q` sees everything) | Finding things was the platform's thinnest capability relative to the agent-native pitch; FTS5 was already the sanctioned scaling escape hatch; exact-match-only filters blocked range/substring queries agents routinely need | LIKE-scanning `data_json` (banned by D4); Vectorize semantic search (deferred — FTS first, revisit if insufficient); contentless/external-content FTS5 (trigger machinery for no benefit at this scale); a cmd-K palette island (user chose the lighter header-box + results page) |
| D29 | Recoverable delete: deleting a document SNAPSHOTS it (data + newest 20 revisions as `revisions_json`) into `document_trash`, then hard-deletes the original in the same batch — FK cascades clear index/revisions/grants, the FTS row is deleted explicitly. Restore is a DIRECT query re-insert under the ORIGINAL id (relations resume), re-indexed against the CURRENT definition; 409 if the collection is gone or the id/unique value was re-taken. `delete` on the collection gates the whole trash surface (list/restore/destroy — symmetric, no new action), with own/published conditions compiled into the listing query and evaluated on restore from the snapshot's creator/status. 30-day cron purge. Surfaces: /admin/trash, REST /api/trash (+ /:id/restore, DELETE /:id), MCP `delete_<slug>` | Hard delete was unsafe to hand to agents (and humans): cascades destroyed revisions/grants irreversibly. The trash-table model needs ZERO read-path changes — no `deleted_at` guard to forget, no unique-index false conflicts | `deleted_at` soft-delete in place (guards needed in 8+ query sites + compileReadFilter; each miss is a leak; uniqueness false-conflicts) — user-rejected; restore through the create pipeline (definition drift could make documents unrecoverable) |
| D30 | System actor: `'system'` added to `Surface` and `Principal.kind` (closed-vocabulary extension) with a `systemPrincipal()` factory — NO principals row, NO seeded permissions. `authorize()` allows system principals BY KIND (skips permission resolution) but still writes the audit row (surface `'system'`, allowed=1) — "authorize is the only audit writer" survives and every cron-driven action stays attributed in /admin/activity. Revisions written by the system actor carry `saved_by` NULL (no principals row for the FK; the audit row is the attribution) | Scheduled publishing needs a real actor for its audit trail; a permission-resolving pseudo-principal would need seeded rows and could be narrowed by accident, silently breaking the drain | A seeded `system` principals row (the kind CHECK is 'user'/'agent'; a row invites role assignment/token issuance to the platform itself); witness-free direct publishing from cron (bypasses validation/revisions/audit — exactly what D31 reserves for derived-state purges only) |
| D31 | Cron via two Workers triggers — per-minute (scheduled-publish drain, future) and daily 03:00 UTC (maintenance: trash purge; events pruning later) — dispatched on the literal `controller.cron` string in `src/jobs`; `src/main.tsx` exports `{fetch, scheduled}` (the Hono app is a named export for tests). Jobs call SERVICES only; each job try/caught. Retention purges are WITNESS-FREE query functions (maintenance, not authorization decisions — no principal exists in cron; they write no audit rows) | The platform needs a clock (scheduled publish, retention) and Workers cron is the zero-dependency native answer; the jobs module keeps layering intact | Durable Object alarms (a DO for a cron is not lightweight); external schedulers (violates single-Worker deploy) |
| D32 | Scheduled publishing via nullable `documents.publish_at` (+ hand-added partial index, migration 0010): pending ⇔ `status='draft' AND publish_at IS NOT NULL`. Scheduling requires the `publish` action (it IS a deferred publish decision), `hasLifecycle`, and a draft; it is a NARROW write (`setPublishAt` — no data change ⇒ no revision, no index/FTS churn). The per-minute drain selects due drafts witness-free (metadata only, LIMIT 50, oldest first) then publishes each through the FULL `setPublished` pipeline as the system actor (D30) — validation, revision, audit; `publish_at` clears in the same atomic update. Manual publish consumes a pending schedule; a past `publish_at` publishes on the next drain; per-doc failures log and retry next minute. Surfaces: sidebar datetime-local + Schedule/Cancel, REST `POST …/:id/schedule {publishAt\|null}`, generated MCP `schedule_<slug>` (exactly one of publish_at/cancel) | "Publish at 9am Monday" is table-stakes for a CMS and the natural first consumer of the cron infra; a column (not a side table) keeps pending state on the row the drain and editor both already read | A `scheduled_jobs` side table (a second source of truth to reconcile); draining via raw status flip (bypasses validation/revisions/audit); revision-appending schedule writes (pollutes history with no-op data snapshots) |
| D33 | Events outbox: an `events` table of POINTER rows (`type` ∈ document.created/updated/deleted/restored/published/unpublished, media.created/deleted, collection.created/updated/deleted + collection, resource id, principalId, createdAt — never payload), `seq` INTEGER PRIMARY KEY AUTOINCREMENT (the documented exception to nanoid PKs: the poll cursor must stay monotonic and never recycle after pruning), and **no FKs** (an event must survive its subject's deletion). Rows are written as batch items INSIDE the mutation they describe — services attach an optional `event` to query inputs; single-statement queries (media, collection insert/update) became 2-item batches. Read: `GET /api/events?since&collection&limit` and the MCP `poll_events` tool (offered to every principal) → `{data, nextSince}`, filtered per collection to what the caller can `read` (collectionsWithAction ∪ scope-masked publicRead); narrowing to an unreadable collection returns EMPTY, not 403 (no enumeration). 30-day prune; seq gaps are legal (`since` is a horizon) | Agents need "what changed since I last looked" without diffing full lists; pointers keep the feed leak-free by construction — content is re-fetched through the gated read surfaces | Push webhooks (standing non-goal — delivery/retry/secret infrastructure; polling suits agent loops); payload snapshots (leak data the poller can't read NOW); per-row ACL evaluation on events (events aren't documents — collection-level capability is the decided model, user fork #4) |
| D34 | MCP parity pack: the /mcp body cap is raised to 8 MiB (route-specific `MAX_MCP_BODY_BYTES`; REST stays 1 MiB) so a new `upload_media` tool can carry base64 file content (~6 MiB effective; strict-validated decode in `src/lib/base64.ts`, `Buffer` silently skips bad chars so charset/padding are checked FIRST) — it reuses `uploadMedia` unchanged (MIME sniff, 25 MiB cap, alt-required) and shares REST's 'upload' rate bucket via the extracted `consumeRateLimit`, keyed tokenId-else-IP. Request capabilities (R2 bucket, limiter) thread route→handler→tools as `McpToolContext`. Also generated per collection: `revisions_<slug>` (read-gated) and `restore_<slug>` (update-gated — restore IS an update). With `delete_<slug>` (D29) this closes the MCP↔admin parity gap for content operations | "An agent over MCP can do everything a non-technical admin can" is a stated success criterion (PROJECT_BRIEF); upload/revisions/delete were the holes | A separate binary upload endpoint for agents (second upload pipeline); raising the cap globally (REST/admin don't need it); presigned R2 URLs (bypasses the sniff/alt/audit pipeline) |
| D35 | Public discovery pack, always-on (publicRead is the opt-in; an empty feed is valid): `/rss.xml` (RSS 2.0, one merged site feed over every publicRead+lifecycle collection, published only, 50 items, `?collection=` narrows with an indistinguishable 404), `/sitemap.xml` (`/` + published public docs, cap 5000), `/robots.txt` (protected prefixes + `/s/` disallowed), and `/` upgraded from an admin redirect to a public homepage the moment a publicRead+lifecycle collection exists. Routes hand-registered in main.tsx (dotted filenames — the openapi.json precedent); data assembled by `src/services/discovery` reading AS ANONYMOUS through the gated list pipeline; XML built by pure `src/lib/feeds.ts` (everything escaped); shared def heuristics extracted to `src/lib/def-helpers.ts` (`titleFieldOf` — the same heuristic search/relations already used — `titleOf`, `publicUrlOf`, `excerptFrom`) | Published content was unfindable: no feeds, no sitemap, no index page — the "publishing" half of the platform pitch ended at a URL nobody could discover | Atom (RSS 2.0 has universal reader support; both is maintenance); a `feedsEnabled` setting (settings sprawl — publicRead already expresses the intent); per-def `titleField` override (heuristic first; noted for later) |
| D36 | Per-page `<head>` via `c.render(content, head)` second-arg props: a `PageHead` type (`title/description/canonical/ogType/ogImage/feedUrl`) + a Hono `ContextRenderer` module augmentation in src/layouts.tsx; RootLayout renders OG/canonical/feed tags only when set and falls back to the static branding otherwise, so all pre-existing single-arg `c.render` calls are untouched. Routes compose the FULL title string themselves (`${docTitle} — ${siteName}`) — the layout does no DB reads | OG/social meta needs per-page data but the head is built once in the root jsxRenderer; the second-arg props channel is the idiomatic Hono seam and it typechecks | A `c.set('head', …)` side-channel (stringly, no types); per-route full-document rendering (duplicates the shell); a head-building service the layout calls (layout doing DB reads inverts the layering) |
| D37 | Import/export as NDJSON, one collection per file: line 1 header `{kind:'remill-export', version:1, exportedAt, collection: <full def>}`, then `{kind:'document', id, status, data, createdAt, updatedAt, publishedAt, createdBy}` lines. Export (`GET /api/c/:slug/export` + admin download) is bounded by the caller's COMPILED READ FILTER — you export what you can read. Import (`POST /api/c/:slug/import`, 10 MiB cap, 'import' rate bucket 10/60s, `?dryRun=1`, + admin upload page) upserts by preserved `doc_…` id through the FULL validated pipeline with per-item authorize and per-line errors (never aborts); preserved on create: id/status/createdAt/publishedAt (updatedAt = import time; createdBy = the importer — foreign principal ids don't exist here). SECURITY: lines with `status:'published'` additionally require the `publish` action (import is not a publish bypass). Header def slug must match the target; import NEVER mutates the def. Snapshot: `snapshotSite` (settings button, `manage_schema` '*'-gated — the rebuild-search precedent) writes every collection + media METADATA + manifest.json to R2 `snapshots/<ISO>/`; cron wiring deliberately left as a hook point | Data egress/ingress is a product feature for an agent-native platform (backup, migration, bulk authoring), and it must ride the one validated pipeline — a bypass importer is exactly the Blogmill hole this project exists to close | A JSON envelope (not streamable/diffable/line-addressable); a raw-SQL/bulk-insert import path (bypasses whitelist/unique/authorize); preserving foreign createdBy (dangling FKs, forged attribution); R2 binary copies in snapshots (originals already live in R2 — duplication without a restore story) |
| D38 | Editor islands: (1) CodeMirror 6 markdown island (`src/client/markdown-editor.ts` — **implements D13**; deps exactly `codemirror` + `@codemirror/lang-markdown`, vite-bundled/self-hosted/CSP-safe) — the widget's `data-bind` textarea STAYS in the DOM as the form/signal carrier, hidden `sr-only` (never display:none), synced by dispatching a native bubbling `input` event per doc change; theme maps `var(--color-*)` tokens (light-dark() ⇒ no JS theme branching). (2) A native Dialog media-picker island (`src/client/media-picker.ts` — **supersedes D12/Uppy**): server-rendered fragment at `GET /admin/media/picker` fetched via plain fetch+innerHTML (the repo's `@get`-on-load ban), in-dialog upload via `POST /admin/media/picker` (session-authed, same uploadMedia service + 'upload' rate bucket, returns JSON {id} for select-on-upload); fragment carries NO nested form and only type="button" buttons (it lives inside #editor-form). Scripts load from the two editor ROUTE files only; no-JS degrades to plain textarea + paste-an-id | The plain-textarea markdown editor and paste-a-media-id widget were the two v1 embarrassments (D12/D13 deferred since the foundation plan); both land without a framework, without CDN scripts, and without touching the save pipeline | Uppy (D12 — a resumable-upload framework for a single-file picker; user chose native); Datastar `@get` fragment loading (banned — re-fires/full-page-reloads on load-fragments); TinyMCE/Tiptap (D13 alternatives — heavy, contenteditable fights); iframe-isolated editor (breaks form association) |
| D39 | Bulk admin-list actions via ONE classic native form: `GeneratedTable selectable` adds a leading checkbox column (`name="ids"`, per-row aria-label from the shared title heuristic) + a sticky bulk bar whose buttons are the `op` dispatch (publish/unpublish only when `hasLifecycle` — what setPublished enforces; trash copy notes 30-day recovery, no confirm dialog); select-all is a DOM-convenience Datastar one-liner, the FORM is the state. `POST /admin/c/:slug/bulk` → `bulkDocuments` loops the EXISTING single-item services per id (per-item authorize + audit + revision + outbox event; per-item try/catch — partial failure never rolls back completed items), capped at 100 ids; results return as a `?bulk=ok:<n>,failed:<m>` role=status flash (the repo's query-param flash precedent, not the Toast host). No bulk REST/MCP surface — agents compose the per-item tools | Multi-select then publish/trash is the last high-friction admin flow, and it is safe to ship BECAUSE Phase 2 made delete recoverable; the native form works without JS and sidesteps signal-naming for nanoid ids | Datastar signal-per-row selection (nanoid ids make invalid/awkward signal names; breaks without JS); a bulk service running one big batch (would need new query paths and lose per-item authorize/audit); bulk REST/MCP endpoints (per-item tools already compose) |
| D40 | Removed the in-app site-wide global rate limiter (`app.use('*', rateLimit('global', …))` in main.tsx). It did one `kv.put` per DYNAMIC request (`consumeRateLimit` reads then writes a fixed-window counter), which exhausted the Cloudflare **free-tier 1,000-writes/day** KV budget under ordinary crawler traffic on the discovery pack (`/`, `/rss.xml`, `/sitemap.xml` + `/api/*`). The abuse-prone endpoints keep their own tight per-endpoint limiters (login 10/min, token 20/min, upload 30/min, import 10/min, join 10/min — all still KV-backed via `consumeRateLimit`); volumetric abuse is absorbed by Cloudflare's edge DDoS + zone WAF. `GLOBAL_RATE_LIMIT` (1000/60s) is retained ONLY as the nominal default `apiJson` stamps into `X-RateLimit-*` when a route carries no per-endpoint limiter (the documented v1 header stub — API_AND_MCP_STANDARDS §Rate-limit headers) | A `kv.put` on every request is the wrong cost model for a soft, rarely-hit ceiling: read traffic (the bulk of hits, incl. crawlers) now touches KV zero times, while the endpoints that actually need limiting are unaffected. No migration, no API-contract change (headers still emitted), no test changes | Upgrading to Workers Paid $5/mo purely to keep a soft global cap (pay to preserve the least-valuable tier); sampling the global limiter (1-in-N writes — hacky, weakens the counter); a Durable Object rate limiter (not lightweight, still paid); Cloudflare native Rate Limiting WAF rule (viable belt-and-braces backstop, but a dashboard/ops step outside the codebase — noted as optional follow-up, not this change) |
| D41 | Public render TEMPLATES: an optional per-collection `template` key (new nullable `collections.template` column, migration 0012 — pure `ADD COLUMN`, no CHECK) selects a purpose-built reading layout from a CODE registry (`src/templates/`: keys/types/registry + the `article` template + `lib/conventions`), the field-registry grain applied to the render surface (templates are code; the key is data). The route resolves the template BEFORE the generic `DocumentView` fallback (`renderMode:'raw'` still short-circuits first) and renders it inside PublicShell with a small derived `TemplateContext` (settings, baseUrl, readingMinutes, shareUrl); the SAME resolution runs on `/s/:token` with `shareUrl` OMITTED (a private link never advertises a public share). The `article` template binds fields by CONVENTION (`resolveConventionLayout` — first media→hero, first non-title text→dek/standfirst, markdown→body, tags/relations→meta, slug NEVER rendered), so a co-designed collection needs zero presentation config. It also lands the reader-facing pieces the generic inspector lacked: hero-first with REAL alt (media read-path expansion `expandMedia` — batched `media`-table sibling of data, degrades on Forbidden so a public read never 500s), reading time (`src/lib/reading-time.ts`), a `.rm-standfirst` dek, a masthead home-link+logo, and a progressive-enhancement reader-share bar (copy-link + Web Share island, ≠ the D22/D26 access-control share). The "blog pack" (thin slice) = the `article` template + the `blogCollectionScaffold` definition; no installer machinery yet (deferred until a second pack). Also unified `DocumentView`'s H1 on `titleFieldOf` (was `showInList??fields[0]`) so the on-page title matches OG/feeds | The universal `DocumentView` is a record INSPECTOR (every field labelled, in definition order) — right for the admin detail view, wrong for a designed reading experience (slug shown, excerpt dumped inline, hero wherever its field sat). A layout is a cohesive artifact best expressed as a code template, NOT decomposed into per-field presentation roles smeared across the schema data (an earlier `view.role` draft — rejected on stress-test). Templates-as-code-selected-by-a-data-key is the engine's own grain (field types are code, collections pick them) applied to rendering, and it keeps "blog-ness" in an opt-in pack, never in the core render path | Per-field `view.role` hints in `fields_json` (presentation in the data model; a layout smeared across field tags — user-rejected); a stored template DSL / bring-your-own-markup (that is already `renderMode:'raw'` — no new sandboxed template language); a third `renderMode:'article'` enum value (would force rewriting the `render_mode` CHECK; `template` is orthogonal to raw and open-ended as templates grow in code); hardcoding `if collection==='articles'` (betrays the universal engine); wiring reading time through a per-field `beforeRender` hook (wrong altitude — it is a document-level derived value; the template/context layer is the right home, and `beforeRender`-into-public stays a separate deferred capability) |
| D42 | Content-pack MARKETPLACE: a closed pack registry (`src/templates/packs.ts` — `Pack` = key/name/description/template + co-designed `collections: CollectionDefinition[]`, array from day one for future multi-collection bundles) made reachable on every surface: MCP `list_templates`/`list_packs` (ungated discovery, SEC-5 posture) + `install_pack` (manage_schema-gated), REST `GET /api/templates` + `GET /api/packs` + `POST /api/packs/:key/install` (hand-added to `staticPaths()`), and the admin **Marketplace** page (`/admin/marketplace`, new nav item, admin-only in NAV_BY_ROLE). All three call ONE `installPack` service that authorizes every target slug FIRST (no conflict-vs-forbidden existence oracle for unauthorized callers), pre-flights ALL slugs for conflicts (all-or-nothing — D1 writes are not transactional across collections), then loops the ordinary `createCollection` (validateDefinition + `collection.created` outbox event inherited for free). Optional `slug` override renames a single-collection pack's scaffold. Companion Phase-2 hardening: per-template `wants` capability flags gate the share island + reading time; `bind` ({title/hero/lead}→field key, new `bind_json` column, migration 0013) is the explicit-binding escape hatch convention falls back from; `titleFieldOf` prefers text over slug and honors `bind.title` | The D41 blog pack was a TypeScript constant reachable by nothing but its own test — invisible to the platform's headline persona (an agent discovering remill over MCP had no path to learn templates or packs exist). Making packs first-class delivers the CLAUDE.md promise (define a content type over MCP, then fill it, no deploy) as one tool call, and the shared-service shape keeps the surfaces behaviorally identical per API_AND_MCP_STANDARDS | A `create_collection_from_pack` name (verbose; `install_pack` matches the marketplace mental model); admin-only flow without MCP (contradicts agents-first); a new `install` action in the closed ACTIONS vocabulary (manage_schema already covers schema creation; no new action needed); seeding packs as DB rows (packs are taste-critical code artifacts — the registry stays closed like templates/fields) |
| D43 | The **Overprint rebrand**: "Ink & Paper" (warm paper, iris accent, book serif) is replaced by a risograph two-ink identity — cream stock / ink-navy "gig poster" dark, riso-blue working ink (cyan in dark), fluoro-pink pop ink rationed to EVENTS (denials, publish moments, marketing pop), and their overlap violet carrying the mark below 24px (nib, favicon — the continuity thread to iris). One token set in `tailwind.css` with two load-bearing mechanics: `accent-fg` becomes THEME-AWARE (`light-dark(#fff, navy)`) so every accent fill is AA in both themes, and `ink-subtle` is tuned AA on the `hover` fill because axe evaluates hovered rows. Display face = self-hosted **Bricolage Grotesque** (latin variable woff2 ~30KB, `font-src 'self'`, preload+swap); the `font-serif` utility is renamed `font-display` (22-file codemod). New `Stamp` primitive (affirm/event/refuse; label carries valence; tilt rationed) stamps workflow gates and audit denials. The marketing footer band is theme-FIXED dark — the one sanctioned literal-colour surface. Emails mirror the LIGHT palette by hand (no `light-dark()` in mail; the `#0078bf` test pin is the drift tripwire). `scripts/check-contrast.mjs` audits the promised pairing table. Shipped as five stacked PRs (P0 kit → P1 tokens → P2 homepage+Stamp → P3 sweep → P4 docs) | The owner wanted a full brand overhaul after two mock rounds (artifact-hosted concept comparisons); Overprint won for having a real point of view while structurally preserving what worked — the mark's geometry, the token architecture, the a11y contract. One token source meant the entire admin+public re-skinned in a single mechanical PR | Keeping iris/Ink & Paper (owner explicitly moved on); a per-surface second palette (kraft admin — two AA matrices to maintain); pink as the working accent (fails body AA everywhere); runtime font CDNs (CSP + offline dev); keeping the serif (the grotesque IS the brand voice) |
| D44 | The MCP **`prompts` primitive**: items of prompt-shaped collections (`def.template === 'prompt'` — the D41 template key doubles as the semantic marker) served as native MCP prompts via `prompts/list`/`prompts/get` (`src/mcp/prompts.ts` + two handler cases beside `resources/*`; `prompts: {}` added to initialize capabilities). Prompt name = `<collection>/<item-slug>` with `doc_…` id fallback; arguments derive from declared-∪-scanned `{{variables}}` (`src/templates/lib/prompt-shape.ts` — the SAME pure contract the `prompt` reading template renders, so the picker and the page can never disagree); interpolation is token split/join (`$&`-safe), missing args stay verbatim (all optional). Discovery intersects the principal's permissions via the exported `couldDo` (token scope mask honored); reads go through `listDocuments`/`getDocument(BySlug)` so authorize + the compiled filter apply; published only. Unknown/draft/non-prompt/forbidden collapse to ONE `-32602` (no existence oracle). Companion pack: `prompts` (D42 pack #5) ships PRIVATE by default — the one scaffold without `publicRead` | Prompt libraries are the natural first "agent-native content type": any MCP client (Claude Code, Desktop, …) surfaces the library in its native prompt picker with typed arguments — a capability no headless CMS exposes, and D18 (we own the JSON-RPC dispatch) makes it ~150 lines. Marking prompt-ness by template key keeps renamed installs and hand-built defs participating with zero extra schema | A hard-coded `prompts` slug marker (breaks renamed installs; template key already carries the semantics); a new `prompt` field type or collection shape (heavier schema for the same signal); `listChanged` notifications (no push channel — clients poll); pagination cursors (single-tenant scale; spec allows single-page); revision-pinned prompt fetch (deferred to backlog with the eval-set pack) |
| D45 | The **Nebulae graph explorer** (`/admin/graph`): a zero-dependency 3D canvas island renders every readable item as a star — collections cluster as tilted galactic disks with additive haze, intra-collection relations are local threads, cross-collection relations lift out of the plane as arcing flight paths; pop pink is spent ONLY on the hover/selection event, drag orbits, idle auto-drifts, click opens the item. The plate wraps in `rm-dark-act`, so the graph is the product's dark act in both admin themes; the island resolves tokens ONCE through a probe element (`light-dark()` values are unresolvable via `getPropertyValue` — the probe computes against the subtree's `color-scheme`). Data plumbing: `listGraphNodes` (per-collection node projection — title via `json_extract`, full `data_json` never transfers) + `listAllEdges` (ONE access-blind scan of `document_index` over indexed relation `(collection, field_key)` pairs, covered by `document_index_text_idx`), wrapped by the `graphData` service that owns per-collection `authorize()` + compiled read filters and intersects edges against the visible node set (backlinks' invisible-never-a-leak posture). Caps 1500 nodes / 4000 edges with an honest `truncated` stamp. Payload embeds via `jsonForScript` with edges as `[srcIdx,tgtIdx]` pairs; a11y = canvas `role="img"` with real counts + a visually-hidden per-collection summary (deliberately NOT 1500 focusable links); reduced-motion self-gated (static frame, interaction-driven renders — load-bearing for the e2e axe sweep) | The knowledge-graph story (Track B relations, backlinks) had no visual surface; the owner chose the Nebulae treatment from a two-round artifact proof sheet (flat riso plates rejected for a 3D universe). A canvas island fits D38 exactly: zero deps, route-scoped, progressive | Force-directed layout (hairballs at scale; cluster disks are deterministic and stable across visits via per-id seeded placement); WebGL (canvas 2D is ample at the cap); SSR-embedded per-node link list (keyboard trap); a `/admin/graph/data` fetch endpoint (recorded escape hatch — embed wins at single-tenant scale); theme observer (the dark act makes tokens invariant) |
| D46 | **Private collections** (`access.private`): a second collection-level access flag, mutually exclusive with `publicRead` (rejected together on write), that removes the collection from every DISCOVERY surface — `GET /api/collections[/:slug]`, MCP `list_collections`, `/api/openapi.json` paths+schemas, and pack `installed`-status — for principals holding neither `manage_schema` nor a role/token-scope `read` on it. Content authorization is untouched (non-public collections were already deny-by-default for anonymous); this closes the residual leak that a non-public collection's existence + field shape were visible to everyone via the discovery projection (found probing live `/mcp` 2026-07-18 — the `prompts` collection). Visibility is capability-based, not merely anonymous-hiding: an agent token scoped to `articles` cannot see `prompts` exists. The rule lives once (`canDiscover` + the extracted pure `collectionsWithActionFrom`, resolving permissions once — TD-3); `getCollectionForDiscovery` returns `null` (indistinguishable from nonexistent — no enumeration oracle); `generateOpenApi` became a pure below-services function fed the caller's `listDiscoverableCollections`; `listPackStatuses` gained a principal so `installed` is caller-scoped (a private pack collection reads `installed:false` to callers who can't see it — anyone who could disprove that via `install_pack` holds `manage_schema` and sees truth). Item grants deliberately don't confer discovery (matching MCP tool visibility); share links (`/s/:token`) still work on private collections. Admin UI: one 3-way Visibility select (Public/Discoverable/Private) replaces the publicRead toggle, making the contradictory combo unrepresentable. The `prompts` pack scaffold now pins `access:{private:true}` | A non-public collection hiding its documents but advertising its existence + full field schema to anonymous callers is a real information leak — a private prompt library's schema is sensitive. `access_json` is a JSON column so the flag needs no migration; capability-based visibility keeps discovery consistent with the tool/resource surfaces that already gate on read | Hide-from-anonymous-only (rejected — a narrowly-scoped agent token must not learn the collection exists either); per-collection item-grant probing on every discovery call (rejected for cost + consistency with `couldDo`, which ignores item grants); a dedicated `private` DB column (unnecessary — JSON round-trips); leaving OpenAPI ungated (rejected — it emitted the same schema leak and would hollow the feature) |
| D47 | **The collab pack + text renders** — remill as a multi-model context bus. (a) The `collab` pack (first MULTI-collection pack; the one-template-per-pack test guard deliberately relaxed to "every selected template resolves + ≥1 collection carries the signature template"): three private (D46), lifecycle-none collections whose REQUIRED fields are the handover protocol — `tasks` (stage select — `status` is a reserved field key — plus an optional `open_questions` rollup labelled "Needs a human"; template `status` renders the share-link status page), `warps` (immutable session handovers: state/open_questions/next_action required, so a lazy handover 422s at write time; template `warp`), `decisions` (verdict select note/recommended/blocking; reuses `docs`). Convention: the task carries the CURRENT rollup (updated at warp-out), warps stay historical — templates stay pure, backlinks carry only titles. (b) **Text renders**: per-TEMPLATE named markdown views (`src/templates/renders.ts` — `warp` declares `reviewer`/`implementer`), role-first section ordering with priority-based budget truncation (`budget` ≈ tokens × APPROX_CHARS_PER_TOKEN); exposed as `render`/`budget` args on generated `get_<slug>` tools (advertised ONLY when the template declares renders) and REST `GET /api/c/:c/:id?render=` returning `text/markdown` (+ OpenAPI params). One service (`renderDocumentText`) behind both doors: render name validated against code metadata BEFORE the doc read (no oracle), rides the existing `read` action, unknown name 422s listing what IS available. MCP transport gained ONE narrow branch: an `McpTextResult` sentinel bypasses JSON-stringify so markdown arrives with literal newlines. `open_questions` is markdown, not tags (prose questions exceed the 100-char tag cap; FTS-searchable) | Multi-model handoffs (Claude Code → Codex → any MCP client) have no structured, validated, audited exchange layer; the schema engine already IS the protocol enforcer, and `def.template === 'prompt'` (D44) proved template-keyed non-HTML surfaces. Tailoring is by ROLE + BUDGET only — vendor personas would rot with every model release | Orchestration in core ("the bus, never the brain" — a manager is just another agent on the bus); webhooks v1 (human-relayed; `poll_events` D33 covers catch-up); LLM-composed handovers server-side (the OUTGOING session has the context; remill enforces structure and re-cuts views); Backlink field-data excerpts for the status page (grows `getBacklinks` for one feature — deferred); JSON-quoted render output (escapes every newline — bloats the tokens a budget exists to save) |
| D48 | **Agent connect: OAuth 2.1 AS for MCP + one-step connect wizard + device pairing.** (a) remill is now its own OAuth 2.1 authorization server (MCP authorization spec, 2025-06-18): RFC 9728 protected-resource metadata + RFC 8414 AS metadata (served on all client-probed aliases incl. `/mcp`-suffixed variants and `openid-configuration`), RFC 7591 dynamic client registration (public clients only — no secret column exists), PKCE-S256-mandatory auth-code grant, rotating refresh tokens with one-slot reuse detection, RFC 7009 revocation, RFC 8628 device pairing (`/oauth/device`, XXXX-XXXX codes). Any MCP client connects by URL alone: unauthenticated `/mcp` now 401s with `WWW-Authenticate: Bearer resource_metadata=…` (anonymous MCP is GONE — its only reach was publicRead read-tools, all still on REST/public pages). **OAuth access tokens are ordinary `api_tokens` rows** (`rmo_` prefix, 1 h TTL, new `grant_id` cascade column) — `resolvePrincipal`, token listing, and revocation all work unchanged. **SEC-8 refined: "issuance authority is always a recorded human consent."** The privilege decision is the consent screen (`/oauth/authorize`, session-authenticated, `manage_access`-gated + agent-refused — the identical gate as `issueToken`; writes THE audit row; offers roles minus `admin`/`anonymous`, default `editor`); it materializes an `oauth_grants` row = the capability ceiling (agent principal + role + refresh window; one grant per client registration — reconnects REUSE the principal, role rows replaced). Everything `/oauth/token` does is credential re-derivation inside that ceiling via witness-free grant-scoped queries (the D32 system-actor precedent) — structurally no path below consent touches `principal_roles` or calls `issueToken`; code replay and refresh reuse kill the live chain; deleting the grant cascades everything. Token-endpoint responses (incl. errors) are raw OAuth JSON via early returns — the documented exception to the app error shape. `scope` is echoed, never an authority. (b) The **connect wizard** (`/admin/access/connect`): one atomic `connectAgent` (principal + role + token in one `db.batch`) + per-client connect cards (Claude Code / `.mcp.json` / Cursor + `cursor://` deep link / VS Code + `vscode:` deep link / Gemini CLI / curl, one tested builder shared with the homepage); reconnect mode `?for=` is the only mint surface. The wizard MAY grant `admin` (the sanctioned path). (c) `/admin/access` demoted to a directory: connect CTA, per-agent connection-health lines from `last_used_at`, `via OAuth` provenance badges. Daily cron purges expired codes/devices/grant tokens, unconsented DCR clients (7 d), dead grants (refresh + 30 d) | Connecting an agent took three admin forms plus hand-editing client config, and header-less clients (claude.ai/ChatGPT connectors) could not connect at all; the MCP client ecosystem already implements the OAuth client side, so one server-side implementation buys URL-only connection everywhere with zero per-vendor integrations | Per-vendor OAuth apps (backwards — clients OAuth into remill); a parallel oauth-token table + resolver (rmo_-as-api_tokens keeps one resolution/revocation model); keeping anonymous /mcp with selective challenge (fragile across clients); role-preset scopes in the OAuth `scope` param (roles never come from client input) |
